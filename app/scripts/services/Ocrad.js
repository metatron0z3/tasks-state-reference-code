'use strict';

/**
 * @ngdoc service
 * @name webClientApp.Ocrad
 * @description
 * # Ocrad
 * Factory in the webClientApp.
 */
angular.module('webClientApp')
  .factory(
    'Ocrad',
    [
      function (

      ) {

        return function(image, arg1, arg2) {

          //return OCRAD(image, arg1, arg2);
        }

      }
    ]);
